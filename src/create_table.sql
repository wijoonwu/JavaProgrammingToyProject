CREATE TABLE MEMBER(
                       MEMBER_ID       VARCHAR(30)  PRIMARY KEY,
                       NAME            VARCHAR(30)  NOT NULL,
                       PHONE_NUMBER  	VARCHAR(30)  NOT NULL
);
